// [NFR-02] PostCSS config cho Tailwind CSS v4
// Sử dụng @tailwindcss/postcss plugin (v4 approach) thay vì tailwindcss plugin cũ (v3)
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
